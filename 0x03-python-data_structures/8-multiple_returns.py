#!/usr/bin/python3
def multiple_returns(sentence):
    if not sentence:
        sentence[idx] = None
    for idx in sentence:
        length = len(sentence)
        first_char = sentence[idx]
    return length
    return first_char
