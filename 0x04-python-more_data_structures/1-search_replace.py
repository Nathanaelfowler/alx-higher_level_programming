#!/usr/bin/python3
def search_replace(my_list, search, replace):
    new_list = my_list[:]
    for replace, search in enumerate(my_list):
        if search == x:
            my_list[replace] = y
    return new_list
